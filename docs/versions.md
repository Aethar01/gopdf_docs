# Documentation versions

Use the version selector in the header to switch between the current `git` documentation and tagged releases.

The `git` version follows the current gopdf `main` branch. Tagged references are generated from the registries in each release tag.
